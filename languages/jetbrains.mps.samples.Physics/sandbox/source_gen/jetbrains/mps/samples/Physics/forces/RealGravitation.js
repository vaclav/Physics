/**
  RealGravitation
  Generated from a ECMAScript model by JetBrains MPS.
*/

// Should include module : Gravitation
function computeRealGravitation1(context) {
  return {
    compute: (() => new Physics.ForceMapper(context, ((context, mapper) => {
      
      return computeGravitation1(context, (((6.67430 * Math.pow(10, -11)) * 1))).compute();
    })).compute())
  };
}
